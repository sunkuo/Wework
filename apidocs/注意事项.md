# 注意事项

## 遇到重复消息原因

### 1. 细节说明
- 具体内容：遇到同一个消息回调多次的情况，请看 `json` 中的 `referid` 字段。
- `referid = 0` 表示原消息。
- `referid != 0` 表示在原消息上操作产生的回调（例如已读操作）。

## 文件下载说明

### 1. 细节说明
- 文件消息有两种：
- 外部联系人发送的文件消息（如个微发送的图片/视频/文件），走“外部联系人下载接口”。
- 外部企业好友或内部联系人发送的图片/视频/文件/语音，走 `cdn` 文件下载接口。
